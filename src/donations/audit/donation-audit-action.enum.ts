/** Actions for `donation_audit_logs` (donation box will use `donation_box_donation_audit_logs` later). */
export enum DonationAuditAction {
  UPDATED = "updated",
  STATUS_CHANGED = "status_changed",
  NOTE_UPDATED = "note_updated",
  DELETED = "deleted",
}
