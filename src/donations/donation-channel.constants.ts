/** Offline donation source for bank statement reconciliation imports. */
export const RECONCILIATION_DONATION_SOURCE = "bank" as const;

/** Donation method for donations created via bank reconciliation. */
export const RECONCILIATION_DONATION_METHOD = "reconciliation" as const;
